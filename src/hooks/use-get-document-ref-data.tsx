import { DocumentData, DocumentReference, getDocs, Query } from "firebase/firestore";

const useGetQueryData = <T extends {}>(reference: DocumentReference<DocumentData>) => {

};

export default useGetQueryData;
